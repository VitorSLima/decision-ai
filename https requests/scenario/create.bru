meta {
  name: create
  type: http
  seq: 1
}

post {
  url: {{url}}/scenarios
  body: json
  auth: inherit
}

body:json {
  {
    "title": "Escolha do Fornecedor de Componentes",
    "description": "Comparação entre fornecedores para o próximo lote de produção."
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
